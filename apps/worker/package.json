{
  "name": "@bitspam/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@bitspam/analyzer": "workspace:*",
    "@bitspam/db": "workspace:*",
    "@bitspam/github": "workspace:*",
    "@bitspam/queue": "workspace:*",
    "dotenv": "^17.4.2"
  }
}
